[project]
name = "web-selenium"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "pytest-selenium>=4.1.0",
    "selenium>=4.41.0",
]

[dependency-groups]
dev = [
    "allure-pytest>=2.15.3",
    "ptest>=2.0.3",
]
